{% extends 'base.html.twig' %}

{% block title %}Student index{% endblock %}

{% block body %}
    <h1>Student index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Album number</th>
                <th>Name</th>
                <th>Surname</th>
                <th>Start_year</th>
                <th>Semester</th>
                <th>Card Uid</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for student in students %}
            <tr>
                <td>{{ student.id }}</td>
                <td>{{ student.albumNo }}</td>
                <td>{{ student.name }}</td>
                <td>{{ student.surname }}</td>
                <td>{{ student.startYear }}</td>
                <td>{{ student.cardUid }}</td>
                <td>{{ student.semester }}</td>
                <td>
                    <a href="{{ path('student_show', {'id': student.id}) }}">show</a>
                    <a href="{{ path('student_edit', {'id': student.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('student_new') }}">Create new</a>
    <form action="{{ path('student_upload') }}" method="post" enctype="multipart/form-data">
        <input type="file" name="uploadedfile">
        <input type="submit" value="Upload File"/>
    </form>
{% endblock %}
